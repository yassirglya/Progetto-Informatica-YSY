#include "fileio.h"
#include "utils.h"

void salva_record(const Record *record) {
    // Salva un nuovo record nel file records.txt
}

void leggi_record(Record *records, int *num_records) {
    // Leggi i record dal file records.txt
}

void salva_cronologia(const Giocatore *giocatore) {
    // Salva i dati della partita corrente nel file history.csv
}
